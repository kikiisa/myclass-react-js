# Penginstalan

1. Clone repository ini dengan cara menjalankan perintah `git clone git@github.com:kikiisa/myclass-react-js.git`
2. Masuk ke folder project dengan cara menjalankan perintah `cd core-react`
3. Jalankan perintah `yarn install` untuk menginstalasi semua dependencies
4. Jalankan perintah `yarn start` untuk menjalankan project
