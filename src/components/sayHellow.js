import React from 'react';

// component functional
const SayHellow = (props) => {
    return (
        <div>
            <h1>Hellow {props.name}</h1>
        </div>
    );
};


export default SayHellow;
